// My Second task

// Assigned values to variables
const total = 100;
const ordered = 50;

// Сompare values (100, 50) and output to the console
if  (ordered > 100) {
    console.log("Not enough items on the stock!")
} else if ((ordered < 100) && (ordered > 0)){
    console.log("The order is completed, the manager will contact you")
} else {
    console.log("Err!")
}
